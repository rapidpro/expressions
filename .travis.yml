language: python
python:
    - '2.7'
install:
    - pip install -r requirements/tests.txt --upgrade
    - sudo apt-get install maven2
script:
    - cd python && nosetests -s && cd ..
    - cd java && mvn clean test && cd ..
